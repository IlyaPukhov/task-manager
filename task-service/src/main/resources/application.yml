server:
  port: 8082
spring:
  application:
    name: task-service
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://${CONFIG_SERVER_USER}:${CONFIG_SERVER_PASSWORD}@localhost:8888