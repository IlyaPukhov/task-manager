spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
      hbm2ddl.auto: validate
  jackson:
    property-naming-strategy: SNAKE_CASE
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: false