spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
      hbm2ddl.auto: validate
  cloud:
    config:
      enabled: false
  jackson:
    property-naming-strategy: SNAKE_CASE
eureka:
  client:
    enabled: false